# AGENTS.md

## Cursor Cloud specific instructions

This repository (`servicenow`) is a study-content repository for ServiceNow certification preparation. It currently contains no application code, services, or build tooling.

- **No dependencies** to install — there is no `package.json`, `requirements.txt`, `Makefile`, or similar.
- **No services** to run — there is no application, backend, frontend, or database.
- **No tests or lint** — there are no test frameworks or linting configurations.
- **No build step** — there is no compilation or bundling.

If application code or study tools are added in the future, update this file with relevant setup and run instructions.
